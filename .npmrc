# Configuration NPM pour publication automatique

# Registry NPM officiel
registry=https://registry.npmjs.org/

# Configuration pour les scopes (si nécessaire)
# @yourscope:registry=https://registry.npmjs.org/

# Accès public par défaut
access=public

# Engine strict (correspond au package.json)
engine-strict=true

# Pas de package-lock dans NPM
package-lock=false

# Logs de niveau info
loglevel=info

# Timeout pour les opérations longues
timeout=300000

# Configuration pour le cache
cache-max=86400000

# Configuration pour les builds
rebuild-bundle=true
